- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/bin: bin
    ~/.pandoc: pandoc
    ~/.maid: maid
    ~/.config/hotstrings.json: hotstrings.json
    ~/.fonts : fonts
    ~/.config/nvim : nvim
    ~/.dmenurc : dmenurc
    ~/.enscript : enscript
    ~/.config/fish : fish

