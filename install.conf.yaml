- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/bin: bin
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.pandoc: pandoc
    ~/.maid: maid
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases
    ~/.config/hotstrings.json: hotstrings.json
    ~/.config/i3/config: i3config
