#!/usr/bin/env sh

filename=$1

if	[ -z "$filename" ]
then
	echo "Please enter a filename. The only arg is the filename"
	exit 0
fi

i3-msg workspace "5";zathura $filename &
echo "Opened ${filename} in zathura"
