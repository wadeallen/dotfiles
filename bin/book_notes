#!/bin/bash

filename=$1

if	[ -z "$filename" ]
then
	echo "Please enter file name"
	exit 0
fi


fileroot=$(echo ${filename%.*})

pandoc ${filename} -o ~/Desktop/${fileroot}.pdf --template=book_review.tex --bibliography=books.bib --filter pandoc-citeproc


