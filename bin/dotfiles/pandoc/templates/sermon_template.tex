\documentclass[Letter, 12pt]{report}

%manages margins
\usepackage[margin=1in]{geometry}

%to include links
\usepackage{hyperref}

% added tightlist
\providecommand{\tightlist}{%
    \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%for headings
\usepackage{titlesec}

%prevent page breaks after chapters
\makeatletter
\renewcommand\chapter{\par%
  \thispagestyle{plain}%
  \global\@topnum\z@
  \@afterindentfalse
  \secdef\@chapter\@schapter}
\makeatother

%format headings for chapters and sections
\titleformat{\chapter}{%
\centering\normalfont\LARGE\bfseries}{\thechapter}{1em}{}

\titlespacing*{\chapter} {0pt}{3.5ex plus 1ex minus .2ex}{2.3ex plus .2ex}


% title not on separate page
\let\endtitlepage\relax 

\font\manual=manfnt
\newcommand{\MF}{{\manual META}\-{\manual FONT}}

%keeps the paragraphs from indenting and gives them a 10pt line in between them
\setlength{\parindent}{0in}
\setlength{\parskip}{10pt} 

% % Pull out author and put into Latex Environment
% \newcommand{\earlyreader}{$reader.early$ }
% \newcommand{\latereader}{$reader.late$ }

$if(title)$
\title{$title$}
$endif$
\author{$for(author)$$author$$sep$ \and $endfor$}
\date{$date$}

% changes font to sans-serif

\renewcommand{\familydefault}{\sfdefault}

\begin{document}
$if(title)$
\maketitle
\textsc{Scripture}: $scripture$ \\
$summary$
$endif$ 

\pagebreak

$body$

\end{document}
