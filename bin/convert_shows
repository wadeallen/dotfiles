#!/usr/bin/env bash 


# find . -name "*.mkv" -print | while read filename
# do
#   fileroot=$(echo ${filename%.*})
#   ffmpeg -i ${filename} -s 720x480  ~/Desktop/${fileroot}.mp4
#   echo "--------------------"
#   echo "$fileroot converted"
# done

for filename in *.mkv; do
    fileroot=$(echo ${filename%.*})
    ffmpeg -i ${filename} -s 720x480  ~/Desktop/${fileroot}.mp4
    done

