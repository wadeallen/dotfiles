#!/bin/bash

git add . &&
git commit -m "updated site" &&
git push

if [[ $PWD =~ /home/wadeallen/Sites/fbcmuncie_2020 ]]; then
	cd /home/wadeallen/fbcmuncie_compiled &&
	git add . &&
	git commit -m "updated site" &&
	git push
fi
