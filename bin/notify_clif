#!/usr/bin/env python3

import webbrowser
from datetime import date
from dateutil.relativedelta import relativedelta, TU
import urltools
import subprocess
import sys

format = "%A, %B %d"
date = input("Date of Dedication: ")
baby = input("Name of Child: ")
email_address = 'clif@fbcmuncie.org, TwoDDavis@aol.com'

subject = 'Baby Dedication on ' + date
message = ('Hello Clif and Dana%0A%0A' \
	'I wanted to let you know that we are going to be dedicating ' + baby + ' on ' + date + ' %0A%0A' \
        'Clif, I have added it in planning center. Dana, if you are able to get a prayer shawl to me, I will give it to them along with a dedication certificate.%0A%0A' \
	'Have a great week, %0A%0A' \
	'Wade')


url = 'https://mail.google.com/mail/?view=cm&fs=1&to=' + email_address + '&su=' + subject + '&body=' + message

# webbrowser.open(url)
subprocess.call(["xdg-open", url])

