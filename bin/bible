#!/bin/bash

PASSAGE=`cat /home/wadeallen/Next/books.txt | rofi -dmenu -i -p Scripture:\  $*`
PASSAGE=${PASSAGE// /+}

if [ -z "$PASSAGE" ]; then
  exit
else
  xdg-open "https://www.biblegateway.com/passage/?search=${PASSAGE}&version=NIV&interface=print"
fi


