#!/usr/bin/env bash

DATE=`date +%Y-%m-%d`

touch $HOME/Next/Journal/$DATE.journal.markdown &&
echo -e "Date: $DATE" > $HOME/Next/Journal/$DATE.journal.markdown &&
vim $HOME/Next/Journal/$DATE.journal.markdown
