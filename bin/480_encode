#!/bin/bash

filename=$1

if	[ -z "$filename" ]
then
	echo "Please enter file name as first variable"
	exit 0
fi


fileroot=$(echo ${filename%.*})

HandBrakeCLI -Z Andriod -i ${filename} -o ~/Desktop/${filename}

