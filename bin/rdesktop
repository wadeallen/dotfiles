#!/bin/bash

IP=$(hostname)

if [ $IP == 'wade-desktop' ]
then
  /home/wadeallen/bin/rdesktop 10.1.10.165 -u Staff -p fbc309 -g 80%
else
  /home/wadeallen/bin/rdesktop church.wadeallen.me -u Staff -p fbc309 -g 80%
fi

