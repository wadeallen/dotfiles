#! /bin/bash


if [ -z "$1" ]
  then
	  echo "Put translation (lowercase, niv or nte) as first argument, passage as second argument"
  exit 1
fi

TRANS=$1
PASSAGE=$2

scrape_${TRANS} "${PASSAGE}" | xclip -selection c
