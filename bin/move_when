#!/bin/bash

filename=$1

if	[ -z "$filename" ]
then
	echo "Please enter file name"
	exit 0
fi


fileroot=$(echo ${filename%.*})

scp ${filename} $SHUTTLE:/home/wadeallen/Media/Media/TV/When_Calls_The_Heart/


