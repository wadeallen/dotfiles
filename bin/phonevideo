#!/bin/bash

filename=$1

if	[ -z "$filename" ]
then
	echo "Please enter the .MTS file"
	exit 0
fi


fileroot=$(echo ${filename%.*})

ffmpeg -i ${filename} -vf "scale=-1:360" ~/Desktop/${fileroot}.mp4


